import * as configs from '../../cdk-outputs.json';

describe('create user integration tests', () => {
  it('should create the user correctly', () => {
    console.log('file content:', configs);
  });
});
